/* 
https://www.codingninjas.com/codestudio/problems/pair-sum_697295?source=youtube&campaign=love_babbar_codestudio1&utm_source=youtube&utm_medium=affiliate&utm_campaign=love_babbar_codestudio1
Array of size N and int S
return the list of all pairs of elements such that their sum is equal to S

Each pair should be shorted (first value <= second)
list of pairs in acending order of first value

Solution
Run two loops 1. i to n-1 and inside 2. i+1 to n-1
if sum equal to sum push back pair
sort by fn // sort(ans.begin(),ans.end())

*/

#include<vector>
using namespace std;
vector<vector<int> > pairSum(vector<int> &arr, int s){
   vector< vector<int> > ans;
   
   for(int i=0;i<arr.size(); i++ )
   {
   		for(int j = i+1; j<arr.size(); j++) {
        	if(arr[i] +arr[j] == s)
            {
            	vector<int> temp;
                temp.push_back(min(arr[i], arr[j]));
                temp.push_back(max(arr[i], arr[j]));
				ans.push_back(temp);
            }
        }
   }
   sort(ans.begin(), ans.end());
   return ans;
}